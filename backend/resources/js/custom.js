import "@filament/admin"; // Ensure Filament Admin JS loads
import "../bootstrap"; // Load any custom scripts

document.addEventListener("DOMContentLoaded", function () {
    console.log("Filament custom JS loaded!");
});
